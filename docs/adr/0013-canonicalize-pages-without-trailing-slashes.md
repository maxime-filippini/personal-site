# Canonicalize pages without trailing slashes

The rewritten site will use URLs without trailing slashes except for the root path. Existing trailing-slash section URLs will remain reachable through permanent redirects, while current post URLs remain unchanged. This deliberately replaces the site's mixed convention with one canonical rule without breaking bookmarks or inbound links.
